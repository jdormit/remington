{
  "name": "remington",
  "version": "0.0.0",
  "description": "A keyboard input library",
  "main": "index.js",
  "scripts": {
    "prepush": "rm docs/* && npm run build && npm run build:min && npm run build:commit",
    "build": "webpack index.js docs/remington.js",
    "build:min": "webpack -p index.js docs/remington.min.js",
    "build:commit": "git add docs/* && git commit docs/* -m \"Automated build commit\" && git push --no-verify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "keydown",
    "keyup",
    "keypress",
    "input",
    "keyboard",
    "typing"
  ],
  "author": "Jeremy Dormitzer",
  "license": "MIT",
  "devDependencies": {
    "husky": "^0.11.9",
    "webpack": "^1.13.3"
  }
}
